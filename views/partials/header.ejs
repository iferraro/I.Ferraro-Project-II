<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= tabTitle %> </title>
    <link rel="stylesheet" href="stylesheets/style.css">
</head>

<body>
    <!-- Add login UI here -->
    <nav class="navBar">
        <div id="navCol1">
            Put something here?
        </div>
        <div id="navCol2">
            <img src="" alt="logo">
        </div>
        <div id="navCol3">
            <% if (user) { %>
                <p>Welcome, <%= user.name %></p>
                <form action="/logout">
                    <button type="submit" class="">My Posts</button>
                </form>
                <form action="/logout">
                    <button type="submit" class="loginout">Log Out</button>
                </form>
            <% } else { %>
                <form action="/auth/google">
                    <button type="submit" class="loginout">Login with Google</button>
                </form>
            <% } %>
        </div>
    </nav>
    <h1>
        <%= heading %>
    </h1>